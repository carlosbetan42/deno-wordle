{
  "scripts": {
    "build": "deno bundle pokedle.ts > ./dist/pokedle.js",
    "run": "deno run --allow-env=GOD_MODE --allow-write=./solution.txt --allow-net=pokeapi.co pokedle.ts",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "dev": "deno run --watch --allow-env=GOD_MODE --allow-write=./solution.txt --allow-net=pokeapi.co pokedle.ts"
    // GOD_MODE=true deno run -A --import-map ./import_map.json pokedle.ts
  }
}
